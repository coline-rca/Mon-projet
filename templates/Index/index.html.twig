{% extends "base.html.twig" %}

{% block title %}
Accueil
{% endblock %}

{% block css %}<link rel="stylesheet" href="{{ asset("css/index.css") }}">{% endblock %}

{% block body %}


<!-- SHOP -->
        <section class="intro" id="top">

            {% if app.user %}
                <h2>Bonjour {{ app.user.firstName|capitalize }}</h2>
            {% else %}
                <h2>Bienvenu(e) !</h2>
            {% endif %}
            <div class="presentation">
                <div>
                    <p>MaPlante vous propose régulièrement de nouvelles plantes. Petit budget ou dépensier(ère) sans compter, MaPlante propose toutes sortes d'articles qui sauront vous contenter.</p>
                    <p>Alors n'hésitez plus !</p>
                </div>
            </div>
            
        </section>

        
        <section class="shop">
            <h2>Laissez-vous tentez !</h2>
            
            <div class="items">
                {% for product in products %}
                    <div class="item">
                        <div class="image">
                            <a href="{{ path("item", {id: product.id})}}"><img src="{{ vich_uploader_asset(product, 'imageFile1') }}" alt="{{ product.name }}"></a>
                        </div>
                        <div class="descriptif">
                            <div>
                                <h3>{{ product.name }}</h3>
                                <i class="fa-regular fa-heart click"></i>
                            </div>
                            <div>
                                <i class="fa-solid fa-star"></i>
                                <i class="fa-regular fa-star-half-stroke"></i>
                                <i class="fa-regular fa-star"></i>
                            </div>
                            <div>
                                <span>{{ product.price }} €</span>
                                {% if product.stock > 0 %}
                                    <a href="{{ path("cart_add", {id: product.id})}}" class="btn" >Ajouter au panier</a>
                                {% else %}
                                    <p class="btn-disabled" >Rupture de stock</p>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}

            </div>

            <div class="btn-list">
                <a href="{{ path("products")}}" class="btn" >Plus de produits</a>
            </div>


        </section>

        <section class="infos"  id="infos">

            <div>
                <h3>Qui sommes-nous ?</h3>
                <p>
                    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Minus rem explicabo dolore, consequatur reiciendis aliquam officia at molestiae quaerat cumque commodi voluptatem nulla ipsum beatae molestias voluptas voluptatibus eveniet corporis pariatur quod adipisci, sunt ad quia excepturi. Odio dolorum voluptas dolore similique eligendi? Sit, est explicabo iusto porro aperiam optio.
                </p>
                <a href="" class="btn">En savoir plus</a>
            </div>
            <div class="contacts">
                    <h3>Nos contacts</h3>
                    <p>Pour toutes questions, n'hésitez pas à nous contacter par mail ou par téléphone</p>
                    <ul>
                        <li><a href="mailto:"><i class="fa-solid fa-at"></i> mail@mail.com</a></li>
                        <li><a href="tel:+"><i class="fa-solid fa-phone"></i> téléphone</a> (Du lundi au vendredi de 8h à 17h)</li>
                    </ul>
            </div>

        </section>

{% endblock %}